interface SelectedServices {
  dataUpload: boolean;
  invoiceVerification: boolean;
  unlimitedChances: boolean;
}

interface Chamber {
  id: number;
  name: string;
  nameEn: string;
}

interface City {
  id: number;
  name: string;
  nameEn: string;
}
